# Where the Vite dev server proxies /api requests (the NestJS backend).
VITE_API_PROXY_TARGET=http://localhost:3000

# Optional: talk to the API directly instead of via the dev proxy.
# Leave empty to use same-origin /api (recommended in development).
VITE_API_BASE_URL=
